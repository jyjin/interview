{
  "name": "unittest",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "tap test ./test/*.js",
    "test-report": "npm test -- --cov",
    "test-report-browser": "npm test -- --cov --coverage-report=lcov",
    "clean": "rm -rf ./.nyc_output ./coverage"
  },
  "author": "jyjin",
  "license": "ISC",
  "devDependencies": {
    "tap": "^12.5.3"
  }
}